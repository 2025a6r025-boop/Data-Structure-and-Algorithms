#include<stdio.h>
#include<stdlib.h>
#define M 100
int main(){
    int num;
    scanf("%d",&num);
    int list[M];
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    int insertions;
    scanf("%d",&insertions);
    while(insertions!=0){
        int value,pos=0;
        scanf("%d",&value);
        scanf("%d",&pos);
        for(int i=num;i>pos-1;i--){
            list[i]=list[i-1];
        }
        list[pos]=value;
        num++;
        insertions--;
        
    }
    for(int i=0;i<num;i++){
            printf("%d ",list[i]);
        }
    return 0;
}
