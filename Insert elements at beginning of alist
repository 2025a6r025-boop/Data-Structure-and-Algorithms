#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
    scanf("%d",&num);
    int list[num];
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    int value,pos=0;
    scanf("%d",&value);
    for(int i=num;i>pos-1;i--){
        list[i]=list[i-1];
    }
    list[0]=value;
    num++;
    for(int i=0;i<num;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
