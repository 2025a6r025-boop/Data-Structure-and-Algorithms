#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
    scanf("%d",&num);
    int list[num];
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    int value,found=0,pos=0;
    scanf("%d",&value);
    for(int i=0;i<num;i++){
        if(value==list[i]){
            pos=i;
            found=1;
            break;
        }
    }
    if(found==1){
        for(int i=pos;i<num-1;i++){
            list[i]=list[i+1];
        }
        num--;
        for(int i=0;i<num;i++){
            printf("%d ",list[i]);
        }
    }
    else if(found==0){
        for(int i=0;i<num;i++){
            printf("%d ",list[i]);
        }
    }
    return 0;
}
